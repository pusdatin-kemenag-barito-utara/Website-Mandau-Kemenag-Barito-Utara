{
  "name": "mandau-kemenag-monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "dev": "concurrently -n \"BE,FE\" -c \"cyan.bold,green.bold\" \"cd backend && air\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && air",
    "dev:backend:go": "cd backend && go run main.go",
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && go build -o api-mandau main.go"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
